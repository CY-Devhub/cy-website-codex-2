---
import navigatorImage from "../assets/img/CUREO5/CUREO-Therapie-Navigator.jpg";
const lang = Astro.props.lang === "de" ? "de" : "en";
const copy = lang === "de"
  ? {
      heading: "Eine Plattform, sieben Therapiemodule",
      body1:
        "Von Aktivierung bis alltagsnahes Training unterstützen die CUREO-Module konkrete Therapieziele im klinischen Alltag.",
      body2:
        "Mehr als 50 interaktive Übungen in sieben Modulen sind auf sensomotorische, kognitive und perzeptive Rehabilitation ausgerichtet.",
      body3:
        "Alle Inhalte lassen sich patientenindividuell anpassen und nahtlos in bestehende Therapieprozesse integrieren.",
      imageAlt: "CUREO Therapie-Navigator mit Übersicht der VR-Übungen"
    }
  : {
      heading: "One platform, seven therapy modules",
      body1:
        "From activation to activities of daily living, CUREO modules support practical goals in real clinical workflows.",
      body2:
        "More than 50 interactive exercises across seven modules target sensorimotor, cognitive, and perceptual rehabilitation.",
      body3:
        "All content can be tailored to each patient and integrated seamlessly into existing therapy workflows.",
      imageAlt: "CUREO therapy navigator interface and VR exercise overview"
    };
---

<section class="modules" data-reveal>
  <div class="modules-shell">
    <article class="modules-grid">
      <div class="modules-content">
        <h2>{copy.heading}</h2>
        <p>{copy.body1}</p>
        <p>{copy.body2}</p>
        <p>{copy.body3}</p>
      </div>

      <div class="modules-media">
        <img
          src={navigatorImage.src}
          alt={copy.imageAlt}
          loading="lazy"
        />
      </div>
    </article>
  </div>
</section>

<style>
  .modules {
    background: #ffffff;
    padding: 0 1.2rem 1.8rem;
  }

  .modules-shell {
    margin: 0 auto;
    max-width: 1240px;
  }

  .modules-grid {
    background: #fff;
    border: 1px solid #d9e5d2;
    border-radius: 8px;
    display: grid;
    grid-template-columns: minmax(360px, 1.1fr) minmax(320px, 1fr);
    overflow: hidden;
  }

  .modules-content {
    align-self: center;
    padding: clamp(1.2rem, 3vw, 2.2rem);
  }

  .modules-content h2 {
    color: #1d2f1d;
    font-size: clamp(1.4rem, 2.7vw, 2.2rem);
    letter-spacing: -0.02em;
    line-height: 1.1;
    margin: 0 0 1rem;
  }

  .modules-content p {
    color: #425641;
    line-height: 1.56;
    margin: 0 0 0.7rem;
  }

  .modules-media {
    min-height: 420px;
    padding: 0.6rem;
  }

  .modules-media img {
    border-radius: 8px;
    display: block;
    height: 100%;
    object-fit: cover;
    width: 100%;
  }

  @media (max-width: 960px) {
    .modules-grid {
      grid-template-columns: 1fr;
    }

    .modules-media {
      max-height: 420px;
      min-height: 300px;
    }
  }

  @media (max-width: 700px) {
    .modules {
      padding: 0 0.8rem 1.6rem;
    }
  }
</style>
