---
import cureoHandImage from "../assets/img/CUREO5/Hand-CUREO-5.jpg";
const lang = Astro.props.lang === "de" ? "de" : "en";
const copy = lang === "de"
  ? {
      imageAlt: "CUREO Hand-Modul in einer Therapiesitzung",
      heading: "VR-Therapie für sensomotorische, kognitive und perzeptive Rehabilitation",
      body1:
        "CUREO ist eine zertifizierte Therapiesoftware, die motorische, sensorische und kognitive Funktionen digital unterstützt. Mehr als 50 interaktive Übungen in sieben Modulen (Active, Hand, Moto, Cogni, Relax, Activities und ADL) ermöglichen eine individuell anpassbare Therapieplanung.",
      body2:
        "Das System wird in über 300 Kliniken, Rehabilitationszentren und Therapiepraxen eingesetzt. Patienten trainieren mit einem VR-Headset in motivierenden Szenarien. Gamification-Elemente fördern die Therapietreue und steigern die Motivation.",
      cta: "Termin buchen"
    }
  : {
      imageAlt: "CUREO Hand module therapy session",
      heading: "VR Therapy for Sensorimotor, Cognitive and Perceptual Rehabilitation",
      body1:
        "CUREO is a certified therapy software that digitally supports motor, sensory and cognitive functions. More than 50 interactive exercises across seven modules (Active, Hand, Moto, Cogni, Relax, Activities and ADL) enable individually customizable therapy planning.",
      body2:
        "The system is used in over 300 clinics, rehabilitation centers and therapy practices. Patients train with a VR headset in motivating scenarios. Gamification elements promote therapy adherence and increase motivation.",
      cta: "Book appointment"
    };
---

<section class="leading" data-reveal>
  <div class="leading-shell">
    <article class="leading-grid">
      <div class="leading-media">
        <img src={cureoHandImage.src} alt={copy.imageAlt} loading="lazy" />
      </div>

      <div class="leading-content">
        <h2>{copy.heading}</h2>
        <p>{copy.body1}</p>
        <p>{copy.body2}</p>

        <a
          class="book-btn"
          href="https://meetings-eu1.hubspot.com/cureosity/terminbuchungstool-de"
          target="_blank"
          rel="noopener noreferrer"
        >
          {copy.cta}
        </a>
      </div>
    </article>
  </div>
</section>

<style>
  .leading {
    background: linear-gradient(180deg, #f3f9eb 0%, #f8fcf2 100%);
    padding: 1rem 1.2rem 2rem;
  }

  .leading-shell {
    margin: 0 auto;
    max-width: 1240px;
  }

  .leading-grid {
    background: #fff;
    border-radius: 20px;
    display: grid;
    grid-template-columns: minmax(320px, 1.05fr) minmax(340px, 1fr);
    overflow: hidden;
  }

  .leading-media {
    min-height: 420px;
    padding: 0.6rem;
  }

  .leading-media img {
    border-radius: 8px;
    display: block;
    height: 100%;
    object-fit: cover;
    width: 100%;
  }

  .leading-content {
    align-self: center;
    padding: clamp(1.2rem, 3vw, 2.2rem);
  }

  .leading-content h2 {
    color: #1d2f1d;
    font-size: clamp(1.45rem, 2.8vw, 2.4rem);
    letter-spacing: -0.02em;
    line-height: 1.1;
    margin: 0 0 1rem;
  }

  .leading-content p {
    color: #425641;
    line-height: 1.56;
    margin: 0 0 0.7rem;
  }

  .book-btn {
    background: linear-gradient(120deg, #7ea43d 0%, #6d9130 100%);
    border: 0;
    border-radius: 12px;
    color: #ffffff;
    display: inline-block;
    font-weight: 700;
    margin-top: 0.2rem;
    padding: 0.62rem 1rem;
    text-decoration: none;
    transition: transform 160ms ease;
  }

  .book-btn:hover {
    transform: translateY(-1px);
  }

  @media (max-width: 960px) {
    .leading-grid {
      grid-template-columns: 1fr;
    }

    .leading-media {
      max-height: 420px;
      min-height: 300px;
    }
  }

  @media (max-width: 700px) {
    .leading {
      padding: 0.85rem 0.8rem 1.6rem;
    }
  }
</style>
