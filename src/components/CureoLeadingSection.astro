---
import cureoHandImage from "../assets/img/CUREO5/Hand-CUREO-5.jpg";
---

<section class="leading" data-reveal>
  <div class="leading-shell">
    <article class="leading-grid">
      <div class="leading-media">
        <img src={cureoHandImage.src} alt="CUREO Hand module therapy session" loading="lazy" />
      </div>

      <div class="leading-content">
        <p class="eyebrow">CUREO PLATFORM</p>
        <h2>VR Therapy for Sensorimotor, Cognitive and Perceptual Rehabilitation</h2>
        <p>
          CUREO is a certified therapy software that digitally supports motor,
          sensory and cognitive functions. More than 50 interactive exercises
          across seven modules (Active, Hand, Moto, Cogni, Relax, Activities
          and ADL) enable individually customizable therapy planning.
        </p>
        <p>
          The system is used in over 300 clinics, rehabilitation centers and
          therapy practices. Patients train with a VR headset in motivating
          scenarios. Gamification elements promote therapy adherence and
          increase motivation.
        </p>

        <a
          class="book-btn"
          href="https://meetings-eu1.hubspot.com/cureosity/terminbuchungstool-de"
          target="_blank"
          rel="noopener noreferrer"
        >
          Book appointment
        </a>
      </div>
    </article>
  </div>
</section>

<style>
  .leading {
    background: linear-gradient(180deg, #f3f9eb 0%, #f8fcf2 100%);
    padding: 1rem 1.2rem 2rem;
  }

  .leading-shell {
    margin: 0 auto;
    max-width: 1240px;
  }

  .leading-grid {
    background: #fff;
    border: 1px solid rgba(124, 165, 59, 0.34);
    border-radius: 20px;
    box-shadow: 0 12px 28px rgba(88, 126, 50, 0.1);
    display: grid;
    grid-template-columns: minmax(320px, 1.05fr) minmax(340px, 1fr);
    overflow: hidden;
  }

  .leading-media {
    min-height: 420px;
  }

  .leading-media img {
    display: block;
    height: 100%;
    object-fit: cover;
    width: 100%;
  }

  .leading-content {
    align-self: center;
    padding: clamp(1.2rem, 3vw, 2.2rem);
  }

  .eyebrow {
    color: #688247;
    font-size: 0.82rem;
    font-weight: 700;
    letter-spacing: 0.12em;
    margin: 0 0 0.55rem;
  }

  .leading-content h2 {
    color: #1d2f1d;
    font-size: clamp(1.45rem, 2.8vw, 2.4rem);
    letter-spacing: -0.02em;
    line-height: 1.1;
    margin: 0 0 1rem;
  }

  .leading-content p {
    color: #425641;
    line-height: 1.56;
    margin: 0 0 0.7rem;
  }

  .book-btn {
    background: linear-gradient(120deg, #7ea43d 0%, #6d9130 100%);
    border: 1px solid rgba(109, 145, 48, 0.9);
    border-radius: 12px;
    color: #ffffff;
    display: inline-block;
    font-weight: 700;
    margin-top: 0.2rem;
    padding: 0.62rem 1rem;
    text-decoration: none;
    transition: transform 160ms ease, box-shadow 160ms ease;
  }

  .book-btn:hover {
    box-shadow: 0 10px 20px rgba(73, 106, 28, 0.25);
    transform: translateY(-1px);
  }

  @media (max-width: 960px) {
    .leading-grid {
      grid-template-columns: 1fr;
    }

    .leading-media {
      max-height: 420px;
      min-height: 300px;
    }
  }

  @media (max-width: 700px) {
    .leading {
      padding: 0.85rem 0.8rem 1.6rem;
    }
  }
</style>
